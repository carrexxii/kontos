(menhir
  (modules parser)
  (flags --explain --dump)
  (infer true))

(ocamllex lexer)

(executable
  (public_name kontos)
  (name main)
  (libraries core core_unix.command_unix menhirLib)
  (preprocess (pps ppx_jane))
  (flags (:standard -warn-error "-A")))
